interface Fix {
    x: number;
    y: number;
    name: string;
}
interface Waypoint extends Fix {
}
interface VORDME extends Fix {
}
interface VORTAC extends Fix {
}
declare function generateWaypoints(): Waypoint[];
declare function generateVORDMEs(): VORDME[];
declare function generateVORTACs(): VORTAC[];
declare function generateFixes(): Fix[];
declare function generateFixesArp(): Fix[];
export { generateFixes, generateFixesArp, generateVORDMEs, generateVORTACs, generateWaypoints, type Fix, type VORDME, type VORTAC, type Waypoint };
